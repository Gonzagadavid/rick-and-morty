import { Box } from '@mui/material';
import { Layout } from 'Layout';
import { RoutersComponent } from 'routes';

function App() {
  return (
    <Layout>
      <Box>
        <RoutersComponent />
      </Box>
    </Layout>
  );
}

export default App;
